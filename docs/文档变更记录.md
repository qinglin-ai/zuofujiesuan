# 文档变更记录

> **用途**：记载所有文档的新增/修改。凡对既有文档做**新增或内容更新**，必须在此登记**变更原因、变更内容、日期**，保证"每次改动可追溯、有据可依"。
> 初始状态：本文档建立时，前期设计已全部定稿融合进正式文档（见"基线状态"），后续所有改动一律由此登记。

---

## 基线状态

> 截至本文档建立，以下内容已融合为正式正文，**不再以"新增/待办/补全"形式存在**：

| 已融合内容 | 落到哪份正式文档 |
|-----------|-----------------|
| 需求与角色、功能清单 | 需求清单.md |
| 任务流程、四套状态机、异常分支 | 业务流程图解.md |
| 全部集合、字段、索引、ER、安全规则 | 数据库结构说明.md |
| 事务/原子自增/唯一索引/权限机制 | 技术机制预研.md |
| 双端页面与状态映射 | 原型与UI设计.md |
| 架构分层、开发周期、阶段分解 | 系统架构与开发规划.md |
| 任务拆解、工时、负责人 | 开发任务拆解表.md |
| 数据库补全字段（reviewedBy/reviewTime/payRefNo/paidSource/target） | 数据库结构说明.md |
| 关键决策（O1~O4：对账模板/违规策略/企业付款/公告定向） | 需求清单.md + 评审问题与待办修复方案.md |

> 评审类过程文档（复核报告/方案评审/阶段冻结/修复方案）仅作**决策依据留存**，其结论均已并入正式文档，不参与后续变更。

---

## 变更登记表

| 序号 | 日期 | 涉及文档 | 变更内容 | 变更原因 | 变更人 |
|------|------|----------|----------|----------|--------|
| 1 | 2026-08-25 | 系统架构与开发规划.md | 技术底座由微信云开发(CloudBase)改为**前后端分离**：前端=微信原生小程序，后端=Python Flask + MySQL（REST API）；更新架构图/分端职责/约束/阶段/分工/风险用语 | 需求决策：搭建前后端分离架构 | 主智能体(agent) |
| 2 | 2026-08-25 | 技术机制预研.md | 四类机制实现由 CloudBase（startTransaction/`_.inc`/唯一索引/安全规则）改写为 Flask/SQLAlchemy/MySQL（会话事务/原子条件UPDATE/唯一约束/统一鉴权+角色校验） | 同步技术底座变更，机制实现层从云函数迁移到 Flask 后端 | 主智能体(agent) |
| 3 | 2026-08-25 | 数据库结构说明.md | 顶部注明集合=关系表及字段下划线映射；第四章改为"权限设计建议"（前端不直连库，后端鉴权层强制） | 数据库由文档型云数据库改为 MySQL 关系表 | 主智能体(agent) |
| 4 | 2026-08-25 | 开发任务拆解表.md | 阶段二 T2-1~T2-4 由"云开发/云函数"改为"Flask 后端脚手架+MySQL 迁移脚本+JWT 鉴权+前端小程序脚手架+CI/CD" | 技术底座变更，任务交付物同步调整 | 主智能体(agent) |
| 5 | 2026-08-25 | 项目进度与文档导航.md | 标记进入阶段二，更新下一步为前后端分离基建 P-A~P-D | 阶段一完成，进入阶段二 | 主智能体(agent) |
| 6 | 2026-08-25 | 项目进度与文档导航.md | 阶段三标志更新：后端核心业务接口（T3-1 注册审批 / T3-2 认证考试 / T3-3 建单 / T3-4 列表详情API / T3-5 领取 / T3-6 提交 / T3-7 核验回写）已完成；前端 T3-4/T3-8 页面待做 | 阶段三后端接口开发完成，需同步整体进度 | 主智能体(agent) |
| 7 | 2026-08-25 | 数据库结构说明.md、业务逻辑说明.md、需求清单.md、业务流程图解.md、数据库设计复核报告.md、原型与UI设计.md、backend/app/models.py、backend/migrations/001_init.sql | **佣金口径调整**：`unitPrice` 由"单个作业佣金"改为"单价（每件作业价格）"；`commissions.amount` 由"=来源任务单价"改为"= 提交数量 × 单价（本周期佣金，一次性入账）"；同步更新 SQL/ORM 注释 | 业务决策：佣金按「提交数量 × 单价」结算，而非旧"单价一条/作业" | 主智能体(agent) |
| 8 | 2026-08-25 | 数据库结构说明.md、需求清单.md、业务流程图解.md、原型与UI设计.md、backend/app/models.py、backend/migrations/001_init.sql、backend/app/api/tasks.py | `tasks` 新增 `description` 字段（任务描述，TEXT 可选）；建单接口接收并入库、列表/详情返回 | 任务详情需要展示描述，补充字段 | 主智能体(agent) |
| 9 | 2026-08-25 | 业务逻辑说明.md | 明确"提交数量"来源：由兼职人员提交作业时**前端上报**，后端据此计算佣金；`commissions.amount` 可由 amount ÷ unitPrice 反算核对，**不新增数量字段** | 业务决策澄清：提交数量不入库，后端逻辑处理 | 主智能体(agent) |
| 10 | 2026-08-25 | 项目进度与文档导航.md | 阶段二标记为**已完成**（✅100%）：Docker 一键部署实机跑通（MySQL 首启自动建库建表、后端健康）、微信登录全链路实联调通过（真实 AppID/Secret→code2session→openid→JWT→前端已登录，/api/health/db db ok）；下一站更新为阶段三前端、阶段四资金链路；下一步新增 push 触发 CI 首次执行项 | 阶段二全链路验证通过（含修复 MySQL8 认证需 cryptography 依赖），进度台账同步 | 主智能体(agent) |
| 11 | 2026-08-25 | 项目进度与文档导航.md、backend/app/api/tasks.py、miniprogram/pages/{tasks,task-detail,my-tasks}/ | **阶段三标为已完成（✅100%）**：新增 `GET /api/tasks/mine` 返回当前用户作业列表及 assignmentId；前端新增任务列表 / 任务详情 / 我的作业三页，实现 T3-4 浏览 + T3-8 领取/提交联动；app.json 注册页面、首页加入口；阶段五备注管理后端接口已就绪、H5 待开发 | 阶段三前后端开发完成，进度台账与接口/页面同步 | 主智能体(agent) |
| 12 | 2026-08-28 | 项目进度与文档导航.md、backend/app/models.py、backend/migrations/002_admin_auth.sql、backend/app/api/{auth,users,certifications,quality,violations,notices}.py、backend/app/{cli,__init__}.py、backend/app/static/admin/{index.html,css/admin.css,js/app.js} | **阶段五「不依赖阶段四」的任务完成**：①管理后台 H5 由 Flask 静态托管（`/admin` 入口）；②admin 账号密码登录——`users` 新增 `password_hash`（迁移002）+ `POST /api/auth/admin/login` + `flask seed-admin` 命令；③用户列表/封禁/解封（`GET /api/users`、`POST …/block|unblock`）；④待核验作业列表（`GET /api/quality-checks/pending-assignments`）；⑤admin 认证列表（`GET /api/certifications/list`）；⑥违规记录+封禁联动（新蓝图 violations）；⑦公告 CRUD（新蓝图 notices）。T5-5 台账/对账导出仍依赖阶段四 T4-5，暂缓 | 执行阶段五中不依赖阶段四的任务，管理后台界面与辅助接口落地 | 主智能体(agent) |
| 13 | 2026-08-28 | 项目进度与文档导航.md、backend/app/api/wallet.py、backend/app/api/quality.py、backend/app/__init__.py、backend/requirements.txt、backend/app/static/admin/{index.html,js/app.js}、miniprogram/pages/wallet/、miniprogram/app.json、miniprogram/pages/index/ | **阶段四资金链路完成（含解锁 T5-5）**：①新增钱包蓝图 `wallet.py`：T4-1 佣金入账（`settle_commission` 幂等+事务+余额原子更新，佣金=提交数量×单价）；T4-2 提现申请扣减（免审核）；T4-3 提现回写（admin 确认到账置 paid）；T4-4 兼职端余额/绑卡/佣金流水/提现记录；T4-5 台账对账导出（按日聚合佣金+提现，含汇总行，支持 CSV/Excel）②quality.py 核验回写 passed 分支自动结算佣金（同事务）③注册 `/api/wallet` 蓝图，requirements 增 openpyxl ④管理后台启用「台账/对账」菜单（T5-5）+「提现确认」菜单（T4-3）⑤兼职小程序新增「我的资金」页；⑥sqlite 内存+test_client 冒烟全链路 PASS | 推进阶段四以解锁阶段五 T5-5，资金链路落地 | 主智能体(agent) |
| 14 | 2026-08-28 | 项目进度与文档导航.md、miniprogram/pages/notices/、miniprogram/app.json、miniprogram/pages/index/、backend/app/static/admin/index.html | ①**兼职端公告查询页**：新增 `pages/notices`（`GET /api/notices`，按角色返回 all/worker 可见公告，含公告/流程/项目动态/FAQ 分类过滤与下拉刷新），首页加入口「公告 / 动态」，兼职端现有四页；②**运营修复**：a) 后台 `login-view` 默认带 `hidden` 类、且 JS 无「无 token 显示登录框」分支，导致无 token 打开 `/admin` 白屏——已去掉默认 `hidden`；b) `docker-entrypoint-initdb.d` 仅在新数据卷首启执行一次，既有 `mysql_data` 卷下迁移 002（`users.password_hash`）未应用，引发 `1054 Unknown column` 致所有需鉴权接口 500，已手动补跑 `002_admin_auth.sql` 修复 | 补齐兼职端公告消费页 + 修复后台白屏与 schema 迁移脱节 | 主智能体(agent) |
| 15 | 2026-08-28 | README.md（新建）、项目进度与文档导航.md | ①**新增项目 README**：概述架构/目录/部署（Docker 与本地）/管理员初始化（seed-admin）/业务与资金口径/安全约定/文档索引；②**明确提现自动打款未联调**：为防误读「阶段四 ✅100%」为含自动打款，进度导航阶段四行改为 `✅100%*`（含 T4-3 为半自动 manual、`paid_source=auto` 已预留），并在顶部新增「已知待开通依赖」警示——自动打款需先开通微信支付商户「企业付款到零钱」权限（D6/O3）后接入 T4-6，商户资质开通前线上提现始终依赖财务后台人工「确认到账」 | 提现为本系统最敏感功能，须在文档醒目标注 T4-6 未联调及商户资质依赖，避免运营误判已自动打款 | 主智能体(agent) |
| 16 | 2026-09-04 | 微信自动打款集成方案.md（新增，2026-09-03）、数据库结构说明.md、业务流程图解.md、业务逻辑说明.md、项目进度与文档导航.md、backend/app/wxpay/（新增包）、backend/app/api/wallet.py、backend/app/api/notify.py（新增）、backend/app/models.py、backend/app/config.py、backend/app/{__init__,cli}.py、backend/migrations/003_wxpay_withdrawal.sql（新增）、backend/requirements.txt（增 cryptography）、backend/certs/.gitkeep（新增）、backend/app/static/admin/js/app.js、miniprogram/pages/wallet/、backend/.env.example + 根 .env.example、docker-compose.yml、.gitignore | **T4-6 微信自动打款（商家转账到零钱）编码完成并通过验证**：①新增 `wxpay` 协议层（APIv3 请求签名/姓名 RSA-OAEP(SHA-1) 加密/回调 AES-256-GCM 解密/防重决策表/mock 客户端）；②提现申请即同步自动发起转账，`withdrawals` 表新增 out_bill_no（唯一）/transfer_bill_no/transfer_status/transfer_time/fail_reason/retry_count 六列（003 迁移）；③**防重铁律**：创建遇 ALREADY_EXISTS/SYSTEM_ERROR/FREQUENCY_LIMIT_*/网络异常先查单，明确 FAIL 才换新单重试（≤1 次），确定性错误直接转人工，绝不盲目换单；④新增微信异步回调 `POST /api/pay/transfer/notify`（验签+解密+幂等+陈旧回调忽略）；⑤管理后台提现列表加「转账状态」列+「失败补发」按钮，小程序资金页细化「待收款确认/转账中」+失败原因；⑥`wxpay-selfcheck` 自检命令 + 30 项防重回归测试全过（SQLite 内存+StaticPool）；⑦同步更新数据库表（withdrawals 新列/索引）、流程（提现状态机/S6/异常分支 E10/E11）、业务规则（提现回写自动为主+防重铁律）与进度导航（T4-6 编码完成、待真实商户凭据实机联调，联调前 `WXPAY_ENABLED=0` 半自动兜底） | 提现自动打款代码逻辑落地，主文档同步其数据库字段/流程/规则/进度，保证文档与代码一致 | 主智能体(agent) |
| 17 | 2026-09-04 | 项目进度与文档导航.md、backend/app/api/tasks.py、backend/app/static/admin/{index.html, js/app.js} | **管理后台新增「任务管理」页（T3-3 前端缺口补齐）**：①后端新增 `POST /api/tasks/<id>/status` 任务状态流转接口（open/inProgress/closed，非法流转 400、同状态幂等 no-op，`require_role("admin")`）；②管理后台侧边栏新增「任务管理」菜单（用户/审批 与 核验管理 之间），页面含发布任务表单（任务名/类型/单人任务量（每人件数）/总人数/单价/难度/要求等级/截止/违禁项/示例图，全部经后端校验）+ 任务列表（状态/领取进度/金额）+ 状态筛选 + 上架·进行中·关闭操作按钮（复用 renderNotices/renderWithdrawals 模式与 ANNOT_LABEL/LEVEL_LABEL 常量）；③打通「管理员发布 → 兼职领取」闭环（此前仅有 T3-3 建单接口、管理后台无发布入口，兼职端任务列表为空）；④端到端验证：建单/列表/状态流转 API 冒烟通过、非法流转 400、UTF-8 中文存储正常、admin 页面与静态资源已重建生效。**已知边界（本次不做）**：任务满员自动转 inProgress 后「重新上架」仍受 `claimed_count==total_people` 限制（无扩量/编辑接口，需另加 `PUT /api/tasks/<id>`）；deadline 无过期自动下架逻辑 | 管理员发布任务入口缺失，兼职端无任务可接，补齐管理后台任务管理页并记录边界 | 主智能体(agent) |
| 18 | 2026-09-04 | 业务逻辑说明.md、项目进度与文档导航.md、backend/app/api/certifications.py、miniprogram/app.json、miniprogram/pages/certify/（新增 4 文件：js/wxml/wxss/json）、miniprogram/pages/{index,task-detail}/*、backend/app/static/admin/js/app.js、backend/app/static/admin/css/admin.css | **能力认证链路补齐（兼职端认证入口 + 管理端指派认证）**：①后端抽取 `_parse_pass_rate` 工具并新增 `POST /api/certifications/grant`（admin 从零指派/覆盖认证，body=openid/task_id/annot_type/level?/exam_passed/pass_rate?，按 openid+任务+类型幂等 upsert，写 level/exam_passed/pass_rate/reviewed_by/review_time；校验：用户/任务存在 404、annot_type/level 非法 400、exam_passed 必须为 bool、pass_rate 0~100）；②小程序新增**能力认证页** `pages/certify`（查看本人认证 `GET /certifications/mine` + 发起认证提交考试成绩 `POST /certifications`，状态展示 待评级/已通过/未通过），app.json 注册；③任务详情页领取按钮按认证资格显隐（`can_claim` 收紧：需 my_cert.exam_passed 且 level≥require_level），未认证显示「去认证」按钮跳 certify?task_id=；④首页「用户」页新增「能力认证」入口；⑤管理后台能力认证页新增「指派认证」表单（用户下拉 `GET /api/users?approval_status=approved&status=active` + 任务下拉 + 类型/等级/是否通过/通过率 → grant），admin.css 补 `.form-tip`；⑥端到端验证：grant 正常/幂等覆盖/用户不存在 404/annot_type 非法 400/exam_passed 非 bool 400/pass_rate 越界 400 全部通过，认证列表与管理后台静态资源已重建生效。**说明**：worker 提交考试成绩（默认 ≥60 通过）→ admin 评级/指派复核，考试仍为「提交成绩→复核」模型（不新增在线答题/题目库）；`submit_exam` 已存在记录仅更新分数、不清理旧等级（既有行为保留） | 兼职端无认证界面导致无法通过准入考试领取任务、管理端无法从零指派认证，补齐认证-领取闭环 | 主智能体(agent) |

> 新增记录示例：
> | 1 | 2026-08-24 | 数据库结构说明.md | tasks 增加 `deadline` 索引 | 支持按截止时间查询 | 张三 |

---

## 变更规则

1. **每次改文档必须有原因**：无业务/设计理由不随意新增字段、页面或规则。
2. **先改正文，后登记**：内容更新同步到正式文档；本表追加一行（含原因）。
3. **同步导航**：若新增了文档或字段、改变了文档职责，同步更新《项目进度与文档导航.md》。
4. **过程文档不改**：评审纪要/复核报告/修复方案为历史快照，结论变更时改正式文档，不回头改历史。