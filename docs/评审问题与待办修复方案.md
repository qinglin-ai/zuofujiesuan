# 左辅云创 · 兼职人员管理小程序 评审问题与跨阶段待办修复方案

> 联动《架构与技术方案评审纪要.md》（Q1~Q6）与《阶段一交付清单与设计冻结.md》（O1~O4）。本文档给出**全部问题的确定修复方案**，其中业务决策类（O1~O4）已与需求方确认并替换开放态。
> 目的：消除阶段一遗留的不确定项，使阶段二/三可开工、阶段四/五有明确实现依据。

---

## 一、评审问题修复方案（Q1~Q6）

| # | 问题 | 严重度 | 修复方案（确定） | 落实阶段 |
|---|------|:----:|------------------|:----:|
| Q1 | 事务单次可写文档数有限，领取高频事务成本高 | 中 | **领取改原子条件自增**：`_.inc(1)` + 条件 `status=open && claimedCount<totalPeople`，不再走读改写事务；事务仅保留在低频资金点（佣金入账、提现扣款）。兜底 = assignment 唯一索引 `(_openid, taskId)` | 阶段三 T3-5 |
| Q2 | 质量追溯仅"通过/未通过"，缺乏打分/复检细节 | 低 | **接受解耦**：本系统以 `quality_checks.externalRefNo` 作为外部系统回查键，打分/复检细节由外部系统提供；本表不扩字段 | 阶段三 |
| Q3 | admin 身份识别依赖 users.role，需强校验 | 高 | **双保险**：① 集合安全规则限制（`doc._openid==auth.openid` 等）；② 敏感写全走**云函数**，云函数内校验 `openid→users.role==admin` 后才放行，禁止客户端直写敏感集合；防伪造 role | 阶段二 P-B / 全期 |
| Q4 | 台账对账导出格式/字段口径未定（=O1） | 中 | **固定 Excel 模板 + 字段口径清单**（见 §二 O1） | 阶段四 T4-5 |
| Q5 | 违规处罚对在途作业/余额处理未定（=O2） | 中 | **不影响已获得资金**（见 §二 O2）：待核验作业继续结算，已入账佣金仍可提现，仅禁止后续领取 | 阶段四前 |
| Q6 | 微信支付打款方式未定（=O3） | 高 | **企业付款到零钱**（见 §二 O3） | 阶段四 T4-6 |

---

## 二、跨阶段待办修复/决策（O1~O4，已收敛）

### O1 台账/对账导出的格式与字段口径 —— 固定 Excel 模板
- **模板结构**（3 个 Sheet）：
  1. **佣金台账**：openid、真实姓名、settleDate、taskId、taskName、assignmentId、单价金额、externalRefNo
  2. **提现流水**：openid、真实姓名、applyTime、金额、收款账户快照、status、paidTime、payRefNo
  3. **汇总页**：累计入账佣金、累计已提、当前可提现余额、按日汇总表
- **聚合口径**：佣金按 `settleDate`（入账日）按日聚合；提现按 `applyTime` 按日聚合
- **导出**：支持日期范围筛选，T4-5 按模板编码

### O2 违规处罚对在途作业与余额 —— 不影响已获得资金
- 封禁（status=blocked）后：**禁止后续领取**新任务
- **在途作业**：已提交待核验的继续按正常核验流程结算（不因封禁作废）
- **已有余额**：已入账佣金仍可正常提现，不冻结
- 实现：`blocked` 只在领取入口（S2）做拦截，不干预既有结算与提现链路

### O3 提现到账方式 —— 企业付款到零钱
- 通道：微信**企业付款到零钱**，实时到账，回调/轮询自动化置 `withdrawals.status=paid`
- 前提：商户号需开通企业付款/现金营销权限
- 回写实现：优先自动回调，失败保留 pending 重试 + 后台人工确认兜底（`paidSource=auto/manual` 区分）
- 补充：需绑定收款账户（openid 接收方），未绑卡不可提现

### O4 公告定向范围 —— 支持定向投递
- notices 增加投递范围字段：`target`（`all` 全员 / `worker` 仅兼职 / `admin` 仅后台 / 或按能力等级）
- 兼职端/后台按 `target` 过滤拉取，实现定向展示
- （本项需在数据库补 `target` 字段，纳入阶段五 T5-7 实现）

---

## 三、状态总结

| 类别 | 项 | 状态 |
|------|-----|:----:|
| 评审问题 | Q1~Q6 | ✅ 全部确定方案 |
| 跨阶段待办 | O1 对账 | ✅ 决定 |
| 跨阶段待办 | O2 违规 | ✅ 决定 |
| 跨阶段待办 | O3 支付方式 | ✅ 决定 |
| 跨阶段待办 | O4 公告定向 | ✅ 决定 |

> 至此阶段一遗留问题全部收敛，无未决项阻塞阶段二/三/四开工。O4 相关 `notices.target` 字段补全已同步至《数据库结构说明.md》。