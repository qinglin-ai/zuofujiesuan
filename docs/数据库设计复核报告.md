# 左辅云创 · 兼职人员管理小程序 数据库设计复核报告（T1-3 交付物）

> 阶段一 T1-3「数据库设计复核并补全」产出。对现行《数据库结构说明.md》做一次**面向实现的复核**，核对其与 T1-2 冻结状态机、T1-1 需求确认（人工准入考试、微信支付接入）的一致性，并给出**字段补全建议**。
> 结论：现有 10 集合设计与冻结状态机基本吻合，无需推翻；本次仅收敛 4 处边界补全（下文 §三），同步纳入后续实现基线。

---

## 一、与冻结状态机的一致性核对

| 集合 | 承载状态机 | 核对结果 | 结论 |
|------|-----------|:----:|------|
| users | approvalStatus(pending/approved/rejected) + status(active/blocked) | ✅ 双字段分离，符合 S2 前置校验 | 一致 |
| balances | availableBalance（可提现余额） | ✅ 唯一冗余字段支撑提现原子扣减 | 一致 |
| certifications | examPassed / level / passRate | ✅ 覆盖「人工准入判定」所需字段 | 需补评审人（见§三） |
| tasks | status(open/inProgress/closed) | ✅ 与 T1-2 任务状态机一致 | 一致 |
| assignments | status(claimed/submitted/passed/rejected) | ✅ 与 T1-2 作业状态机完全一致 | 一致 |
| quality_checks | result(pass/reject) | ✅ 支撑外部核验回写 + externalRefNo 追溯 | 一致 |
| commissions | amount / settleDate / assignmentId(唯一) | ✅ 支撑幂等结算 | 一致 |
| withdrawals | status(pending/paid/rejected) | ✅ 与 T1-2 提现状态机一致 | 需补支付单号（见§三） |
| violations | type / punishLevel | ✅ | 一致 |
| notices | type / createTime | ✅ | 一致 |

> 状态字段枚举值均与《业务流程图解.md》编码一致，无冲突。

---

## 二、索引与并发/幂等约束核对

| 约束目标 | 现有落库 | 核对结论 |
|----------|----------|:----:|
| 并发防超领 / 一人一任务一条 | assignment 复合唯一 `(_openid, taskId)` | ✅ 与 E2 一致 |
| 佣金幂等防重复结算 | commissions `assignmentId` 唯一 | ✅ 与 E5 一致 |
| 核验结果防重 | quality_checks `assignmentId` 唯一 | ✅ 与 E4 一致 |
| 手机号防重复注册 | users `phone` 唯一 | ✅ |
| 余额原子扣减 | 云函数事务内原子更新 | ✅ |
| 对账按日聚合 | commissions 复合 `(settleDate, _openid)` | ✅ |

---

## 三、补全建议（收敛项）

### 3.1 certifications 增加「评审人 / 评审时间」（人工准入判定落地）
人工考试由 admin 审定，需留痕以便追溯。
- 新增字段：`reviewedBy`（评审 admin id）、`reviewTime`（评审时间，`db.serverDate()`）
- 说明：现有 `examPassed` / `level` 由 admin 写入，补操作者与时间即可闭环。

### 3.2 withdrawals 增加「支付单号 / 到账回写来源」（微信支付接入落地）
自动回写依赖微信支付，需记录外部单号便于对账。
- 新增字段：`payRefNo`（微信支付单号/商户单号，可选）、`paidSource`（auto 自动回调 / manual 半自动确认）
- 说明：`status=paid` 时填写；与 E7 半自动兜底对应，`paidSource` 区分回写路径。

### 3.3 commissions 明确「结算周期粒度」与唯一约束范围
- 保持 `settleDate` 记录到日；唯一幂等键为 `assignmentId`（一个作业仅入账一次），不做按天去重。
- 说明：一个作业对应一条佣金，`assignmentId` 唯一即保证同一作业不重复结算（E5）。

### 3.4 tasks 补 `unitPrice` 语义注释
- 保持 `unitPrice` 为单个作业单价（非 quantity×unitPrice）；佣金=unitPrice 一条/作业（存量约定，无需改结构，注释说明即可）。

---

## 四、复核结论

1. **现有数据库结构可复用**，集合划分、状态字段、索引设计均与冻结状态机一致。
2. **需纳入基线 3 处字段补全**：
   - `certifications.reviewedBy / reviewTime`
   - `withdrawals.payRefNo / paidSource`
   - （§3.3/3.4 为注释级收敛，不改结构）
3. **无需新增集合**；微信支付以字段补全承接，不引入支付独立集合。

> 建议将 §三 补全合并回《数据库结构说明.md》，保持单一结构源，供阶段二/三实现直接采用。